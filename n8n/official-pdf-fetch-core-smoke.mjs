import assert from 'node:assert/strict';
import { fetchOfficialFemebalPdf } from './official-pdf-fetch-core.mjs';

const CONTROL_PDF='https://djfhz848yeeat.cloudfront.net/pdf_planillas/5/c/e/5ce377051ea0acb1.pdf';
function workItem(overrides={}) {
  const url=overrides.url??CONTROL_PDF;
  const defaultSource={pdf_url:url,page_url:'https://femebal.com/programacion-fecha-1-torneo-metropolitano-apertura-2026/',source_type:'fecha_normal',phase:'apertura',round_number:1,document_type:'planilla_partido_pdf',provenance:'public_explicit_link'};
  const {source:sourceOverrides={},...itemOverrides}=overrides;
  return {kind:'femebal_official_pdf',method:'GET',allow_redirects:false,auth_used:false,write_enabled:false,url,source:{...defaultSource,...sourceOverrides},...itemOverrides};
}
const pdfBytes=new TextEncoder().encode('%PDF-1.7\nfixture');
const headers=values=>({get(name){return Object.fromEntries(Object.entries(values).map(([k,v])=>[k.toLowerCase(),String(v)]))[String(name).toLowerCase()]??null;}});
function streamingResponse(chunks,{status=200,contentType='application/pdf',contentLength=null,contentEncoding=null}={}) {
  let index=0; let cancelled=false; let released=false;
  const reader={async read(){return index<chunks.length?{done:false,value:chunks[index++]}:{done:true,value:undefined};},async cancel(){cancelled=true;},releaseLock(){released=true;}};
  return {status,headers:headers({'content-type':contentType,...(contentLength===null?{}:{'content-length':contentLength}),...(contentEncoding===null?{}:{'content-encoding':contentEncoding})}),body:{getReader(){return reader;}},state(){return{cancelled,released};}};
}
const fakeResponse=({status=200,contentType='application/pdf',contentLength=pdfBytes.byteLength,contentEncoding=null,body=pdfBytes}={})=>streamingResponse([body],{status,contentType,contentLength,contentEncoding});
let seen=null;
const out=await fetchOfficialFemebalPdf(workItem(),{fetchImpl:async(url,options)=>{seen={url,options};return fakeResponse();}});
assert.equal(out.dry_run,true);assert.equal(out.write_enabled,false);assert.equal(out.auth_used,false);assert.equal(out.source_url,CONTROL_PDF);assert.equal(out.sha256,'f581fc87f30296eff11777c3ce1b9a8b7077071ad8abedfcba317fef0c807224');assert.match(out.sha256,/^[0-9a-f]{64}$/);assert.equal(seen.url,CONTROL_PDF);assert.equal(seen.options.method,'GET');assert.equal(seen.options.redirect,'manual');assert.equal(seen.options.credentials,'omit');assert.deepEqual(seen.options.headers,{Accept:'application/pdf'});assert.equal('Authorization' in seen.options.headers,false);assert.equal('Cookie' in seen.options.headers,false);assert.equal(typeof seen.options.signal,'object');assert.equal(seen.options.signal.aborted,false);
const streamed=streamingResponse([pdfBytes.slice(0,5),pdfBytes.slice(5)]);const streamedOut=await fetchOfficialFemebalPdf(workItem(),{fetchImpl:async()=>streamed,maxBytes:1024});assert.equal(streamedOut.sha256,out.sha256);assert.equal(streamed.state().released,true);assert.equal(streamed.state().cancelled,false);
const oversizedChunk=new Uint8Array(1100);oversizedChunk.set(pdfBytes.slice(0,Math.min(pdfBytes.length,oversizedChunk.length)));const oversized=streamingResponse([oversizedChunk]);await assert.rejects(()=>fetchOfficialFemebalPdf(workItem(),{fetchImpl:async()=>oversized,maxBytes:1024}),/límite real/);assert.equal(oversized.state().cancelled,true);assert.equal(oversized.state().released,true);
let networkCalled=false;await assert.rejects(()=>fetchOfficialFemebalPdf(workItem(),{maxBytes:(12*1024*1024)+1,fetchImpl:async()=>{networkCalled=true;return fakeResponse();}}),/maxBytes inválido/);assert.equal(networkCalled,false);
networkCalled=false;await assert.rejects(()=>fetchOfficialFemebalPdf(workItem(),{timeoutMs:30_001,fetchImpl:async()=>{networkCalled=true;return fakeResponse();}}),/timeoutMs inválido/);assert.equal(networkCalled,false);
let timeoutSignal=null;await assert.rejects(()=>fetchOfficialFemebalPdf(workItem(),{timeoutMs:100,fetchImpl:async(_url,options)=>{timeoutSignal=options.signal;return await new Promise(()=>{});}}),/timeout SAFE/);assert.equal(timeoutSignal?.aborted,true);
let hangingCancelled=false;let hangingReleased=false;const hangingResponse={status:200,headers:headers({'content-type':'application/pdf'}),body:{getReader(){return{async read(){return await new Promise(()=>{});},async cancel(){hangingCancelled=true;},releaseLock(){hangingReleased=true;}};}}};await assert.rejects(()=>fetchOfficialFemebalPdf(workItem(),{timeoutMs:100,fetchImpl:async()=>hangingResponse}),/timeout SAFE/);await new Promise(resolve=>setTimeout(resolve,0));assert.equal(hangingCancelled,true);assert.equal(hangingReleased,false);
await assert.rejects(()=>fetchOfficialFemebalPdf(workItem(),{fetchImpl:async()=>fakeResponse({status:302})}),/Redirect/);await assert.rejects(()=>fetchOfficialFemebalPdf(workItem(),{fetchImpl:async()=>fakeResponse({contentType:'text/html'})}),/Content-Type/);await assert.rejects(()=>fetchOfficialFemebalPdf(workItem(),{fetchImpl:async()=>fakeResponse({contentType:'application/pdfx'})}),/Content-Type/);const withPdfParameter=await fetchOfficialFemebalPdf(workItem(),{fetchImpl:async()=>fakeResponse({contentType:'application/pdf; charset=binary'})});assert.equal(withPdfParameter.sha256,out.sha256);
for(const encoding of ['','   ','gzip','br','deflate','gzip, br']){await assert.rejects(()=>fetchOfficialFemebalPdf(workItem(),{fetchImpl:async()=>fakeResponse({contentEncoding:encoding})}),/Content-Encoding no permitido/,`Content-Encoding ausente de valor o no identidad debe rechazarse fail-closed: ${JSON.stringify(encoding)}`);}const identityEncoded=await fetchOfficialFemebalPdf(workItem(),{fetchImpl:async()=>fakeResponse({contentEncoding:' identity '})});assert.equal(identityEncoded.sha256,out.sha256);
await assert.rejects(()=>fetchOfficialFemebalPdf(workItem(),{fetchImpl:async()=>fakeResponse({contentLength:pdfBytes.byteLength+1})}),/Content-Length no coincide/);for(const invalidLength of ['', '+17','1e3','0x10','17.0','9007199254740992']){await assert.rejects(()=>fetchOfficialFemebalPdf(workItem(),{fetchImpl:async()=>fakeResponse({contentLength:invalidLength})}),/Content-Length inválido/);}const paddedDecimal=await fetchOfficialFemebalPdf(workItem(),{fetchImpl:async()=>fakeResponse({contentLength:`  ${pdfBytes.byteLength}  `})});assert.equal(paddedDecimal.sha256,out.sha256);
let arrayBufferCalled=false;const nonStreamingResponse={status:200,headers:headers({'content-type':'application/pdf','content-length':pdfBytes.byteLength}),async arrayBuffer(){arrayBufferCalled=true;return pdfBytes.buffer.slice(pdfBytes.byteOffset,pdfBytes.byteOffset+pdfBytes.byteLength);}};await assert.rejects(()=>fetchOfficialFemebalPdf(workItem(),{fetchImpl:async()=>nonStreamingResponse}),/streaming acotado/);assert.equal(arrayBufferCalled,false);
networkCalled=false;await assert.rejects(()=>fetchOfficialFemebalPdf(workItem({url:'https://another.cloudfront.net/pdf_planillas/5/c/e/x.pdf',source:{pdf_url:'https://another.cloudfront.net/pdf_planillas/5/c/e/x.pdf'}}),{fetchImpl:async()=>{networkCalled=true;return fakeResponse();}}),/allowlist/);assert.equal(networkCalled,false);
networkCalled=false;const ambiguousBackslash='https://djfhz848yeeat.cloudfront.net\\pdf_planillas/5/c/e/5ce377051ea0acb1.pdf';await assert.rejects(()=>fetchOfficialFemebalPdf(workItem({url:ambiguousBackslash,source:{pdf_url:ambiguousBackslash}}),{fetchImpl:async()=>{networkCalled=true;return fakeResponse();}}),/ambiguos\/normalizables/);assert.equal(networkCalled,false);
console.log('✓ official PDF fetch core SAFE/fail-closed + parser-boundary provenance fixture + host control real + strict PDF media type + identity-only non-empty Content-Encoding + strict decimal Content-Length + bounded streaming-only body + immutable 12 MiB ceiling + abortable 30s timeout with reader cancellation + SHA-256 provenance + raw URL ambiguity rejection OK');
